import { Card, CardContent, Typography } from "@mui/material";

export default function DocumentsWidget() {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">Documents</Typography>
        <Typography>Generate expedition documents.</Typography>
      </CardContent>
    </Card>
  );
}
