import { Card, CardContent, Typography } from "@mui/material";

export default function PurchaseWidget() {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">Purchase Checklist</Typography>
        <Typography>Track purchase completion.</Typography>
      </CardContent>
    </Card>
  );
}
