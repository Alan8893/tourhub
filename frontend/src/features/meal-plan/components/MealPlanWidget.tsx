import { Card, CardContent, Typography } from "@mui/material";

export default function MealPlanWidget() {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">Meal Plan</Typography>
        <Typography>Manage hike nutrition planning.</Typography>
      </CardContent>
    </Card>
  );
}
