# TH-0061.5 — Meal Composition Rules Engine

Status: IN PROGRESS

Last updated: 2026-07-16

## Goal

Implement realistic hiking menu rules on top of the persisted MealSlot model without changing the approved architecture.

## Current model

```text
MealPlan
  ├─ warnings[]
  └─ MealPlanDay[]
       └─ MealSlot[]
            ├─ is_manually_edited
            └─ MealSlotDish[]
                 └─ Dish
                      ├─ selected Recipe
                      └─ DishMealRole[]
                           ├─ is_repeatable
                           └─ allowed MealType[]
```

MealSlot is primary. MealPlanItem remains a legacy compatibility path.

## Persisted classification semantics

ADR-013 defines normalized role and meal-type compatibility owned by `Dish`.

Allowed roles:

- `main`;
- `addition`;
- `drink`;
- `snack`.

Allowed meal types:

- `breakfast`;
- `snack`;
- `lunch`;
- `dinner`.

A Dish may have multiple roles. Each `(dish_id, role)` assignment has its own `is_repeatable` flag and at least one persisted compatible meal type.

Important semantics:

- classification belongs to Dish, not Recipe or MealSlotDish;
- unclassified dishes remain available for manual selection but are excluded from automatic generation;
- no migration or generator may infer roles from names, ingredients, recipes, history, or manual placement;
- manual slots are authoritative because MealSlotDish does not persist the role in which a manually selected dish is used.

## Merged implementation slices

### PR #59 — backend persistence and API

- normalized Dish role and meal-type tables;
- constrained values, cascading foreign keys, and per-role repeatability;
- Alembic revision `h10001` without heuristic backfill;
- atomic classification replacement API and regression coverage.

Quality #197 passed.

### PR #60 — frontend classification

- Russian role and meal-type controls;
- explicit compatible meal types and per-role repeatability;
- local validation, summaries, feedback, and responsive browser coverage.

Quality #206 passed.

### PR #61 — catalogue readiness

- deterministic required and optional coverage evaluation;
- active/classified/unclassified counts;
- archived-recipe exclusion;
- visible Russian warnings and recommendations.

Retargeted Quality #234 passed before merge.

### PR #64 — role-aware generation

- automatic candidates filtered by persisted role and current meal type;
- required `main` and `snack`, optional `addition` and `drink`;
- stable `main → addition → drink` order;
- per-role repeatability and same-day uniqueness;
- archived and unclassified automatic exclusion;
- explicit warnings and no incompatible fallback;
- MealSlot/MealSlotDish primary persistence with MealPlanItem compatibility.

Quality #254 passed. The Product Owner verified the deployed behavior locally.

### PR #65 — canonical baseline synchronization

- status, roadmap, technical debt, project context, domain, architecture, and active task synchronized after role-aware generation.

Quality #256 passed.

### PR #66 — calendar-day three-day diversity for `main`

- pure context records trip day and last selected day per non-repeatable `main` Dish;
- day-one selection is blocked on days two and three and eligible on day four;
- selection count is not used as a calendar proxy;
- only `main` receives the cross-day rule;
- explicitly repeatable `main` bypasses it;
- same-day uniqueness and compatibility filtering remain unchanged;
- exhausted required pools produce deterministic empty slots and warnings;
- pure engine, service, persistence, and public API regressions are included.

Exact-head Quality #271 passed before merge.

### PR #67 — preserve manual MealSlots during regeneration

- Alembic revision `h10002` adds non-null `MealSlot.is_manually_edited` with a safe false backfill;
- successful add, replace, and remove operations mark the complete slot as manually edited;
- removing the final dish records an authoritative empty slot;
- project regeneration reuses the existing MealPlan and rebuilds its scheduled days;
- every marked slot still present in the new schedule is preserved exactly;
- unmarked slots are regenerated through the role-aware engine;
- preserved manual slots bypass automatic composition warnings and diversity rules;
- unclassified manual dishes remain preserved without role inference;
- MealPlanItem compatibility rows are rebuilt from the final composition;
- pure generator, service/persistence, mutation, migration, and public API regressions cover the behavior.

Exact-head Quality #273 passed before merge.

## Current implementation slice

### Draft PR #69 — persist generation warnings

- Alembic revision `h10003` adds a non-null ordered JSON warning snapshot to MealPlan with an empty-list default for existing rows;
- each successful generation assigns the complete warning list produced by that exact generator run;
- subsequent GET responses return the persisted snapshot rather than recomputing it from the current catalogue;
- catalogue changes without regeneration do not rewrite historical generation warnings;
- regeneration replaces the snapshot atomically, including clearing previous warnings to `[]`;
- warning order remains the deterministic generator order;
- public API regression coverage verifies generation, GET persistence, catalogue-only stability, regeneration replacement, and final GET state.

Out of scope for PR #69:

- new candidate-count thresholds;
- preference modes;
- warning inference from current catalogue state;
- frontend behavior changes;
- automatic classification of real deployment dishes.

## Remaining work after PR #69

- maintain and complete explicit classification of the active deployment catalogue;
- define larger candidate thresholds only with approved product requirements;
- preserve purchasing recalculation and legacy compatibility;
- add guided preparation and final release acceptance around the completed generator.

## Constraints

- do not introduce a new service or deployment boundary;
- use only metadata that exists in persistence;
- do not select by role without checking meal-type compatibility;
- do not treat optional addition/drink gaps as blocking readiness;
- do not describe selection count as calendar-day cooldown;
- do not guess catalogue roles or compatibility automatically;
- preserve manual MealSlot assignments and transactional purchasing recalculation;
- keep MealSlot/MealSlotDish primary while legacy MealPlanItem exists;
- treat warnings as the snapshot of the latest successful generation, not a live catalogue-readiness view.

## Next implementation sequence

1. Complete exact-head Quality and review for PR #69.
2. Verify and maintain explicit classification of real deployment catalogue data.
3. Continue TH-0061 guided preparation through shopping, packaging, equipment, and export.
4. Complete release acceptance and deployment documentation.

## Definition of done

- MealSlot remains the primary composition model;
- persisted roles and compatibility match ADR-013;
- borscht cannot become a breakfast candidate unless explicitly classified for breakfast;
- breakfast-only porridge cannot become a lunch or dinner candidate;
- three-day diversity is based on trip calendar days, not selection count;
- repeatable `main` can bypass the three-day restriction;
- manual slots remain authoritative across regeneration without role inference;
- insufficient catalogue warnings remain visible during generation and later reads;
- regeneration replaces stale warnings with the new exact result;
- shopping recalculation remains correct;
- all quality and release-acceptance gates pass.
